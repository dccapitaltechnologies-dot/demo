{
  "name": "local-inter-agent-messaging",
  "version": "1.0.0",
  "description": "A local inter-agent messaging API for AI coding agents",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["agent", "messaging", "cli", "http"],
  "author": "Hyperlane Agent",
  "license": "MIT",
  "dependencies": {
    "express": "^4.18.2",
    "uuid": "^9.0.0",
    "axios": "^1.6.0",
    "commander": "^11.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}